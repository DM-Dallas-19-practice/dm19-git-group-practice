import React, { Component } from "react";

const Tortilla = () => {
  return (
    <button onClick={() => alert("Wheres the salsa?!")}>
      Give Tortilla Chips
    </button>
  );
};

export default Tortilla;
